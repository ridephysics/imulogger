# Keep the help strings in sync with the values in the .py files!
west-commands:
  - file: scripts/west_commands/fw.py
    commands:
      - name: fw
        class: Fw
        help: build the firmware
