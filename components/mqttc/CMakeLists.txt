set(COMPONENT_SRCS
    ${MQTT_DIR}/src/mqtt.c
    mqtt_pal.c
)
set(COMPONENT_ADD_INCLUDEDIRS
    include
    ${MQTT_DIR}/include
)
set(COMPONENT_PRIV_REQUIRES
    uev
    crosslog
)
register_component()
